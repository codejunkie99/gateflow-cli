# Test Project Filelist
# This filelist defines the compile order for the test fixtures

# Include paths
+incdir+.

# Defines
+define+DEBUG
+define+WIDTH=8

# Files in compile order (dependencies first)
defs.svh
types_pkg.sv
counter.sv
interface_example.sv
top.sv
