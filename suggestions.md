# Suggestions

- Add a CLI helper (e.g. `gateflow memory search "<pattern>"`) that runs `rg` over `~/.gateflow` archives/knowledge JSON, then optionally ranks + token-budgets results before injection.
