# Module 2.1: Create Extractor Types

## Overview

| Attribute | Value |
|-----------|-------|
| **File** | NEW: `src/memory/extractors/types.ts` |
| **Purpose** | Type definitions for extraction results and options |
| **Effort** | 10 minutes |
| **Dependencies** | None |

## Purpose

Define TypeScript interfaces for:
1. **ExtractedCount** - Statistics about what was extracted
2. **ExtractionOptions** - Configuration for extraction behavior

## File Location

```
src/memory/
├── extractors/
│   ├── types.ts          ← THIS FILE
│   ├── module-extractor.ts
│   ├── dependency-extractor.ts
│   ├── hierarchy-extractor.ts
│   ├── indexer-extractor.ts
│   └── index.ts
├── KnowledgeStore.ts
├── MemoryService.ts
└── ...
```

## Implementation

### Full File Content

```typescript
/**
 * Types for knowledge extraction from indexer output
 * @module memory/extractors/types
 */

/**
 * Statistics returned after extraction completes
 */
export interface ExtractedCount {
    /** Number of module declarations extracted */
    modules: number;

    /** Number of interface declarations extracted */
    interfaces: number;

    /** Number of package declarations extracted */
    packages: number;

    /** Number of file dependency edges extracted */
    dependencies: number;

    /** Number of hierarchy nodes extracted */
    hierarchy: number;
}

/**
 * Options controlling extraction behavior
 */
export interface ExtractionOptions {
    /**
     * Project identifier (12-char MD5 hash of project path)
     * Used for scoping knowledge to the current project
     */
    projectId: string;

    /**
     * Unique session identifier (UUID)
     * Used for tracking source of extracted knowledge
     */
    sessionId: string;

    /**
     * Whether to extract module/interface/package information
     * @default true
     */
    extractModules?: boolean;

    /**
     * Whether to extract file dependency relationships
     * @default true
     */
    extractDependencies?: boolean;

    /**
     * Whether to extract module hierarchy
     * @default true
     */
    extractHierarchy?: boolean;

    /**
     * Minimum confidence level for extracted items
     * Items from indexer typically have 0.9-1.0 confidence
     * @default 0.9
     */
    minConfidence?: number;

    /**
     * Maximum number of items to extract per category
     * Prevents overwhelming the knowledge store from large projects
     * @default 500
     */
    maxItemsPerCategory?: number;

    /**
     * File patterns to include (glob syntax)
     * If specified, only extracts from matching files
     * @example ['src/**\/*.sv', 'rtl/**\/*.v']
     */
    includePatterns?: string[];

    /**
     * File patterns to exclude (glob syntax)
     * @example ['**/test/**', '**/tb_*']
     */
    excludePatterns?: string[];
}

/**
 * Result of a single extraction operation
 */
export interface ExtractionResult {
    /** Whether extraction completed successfully */
    success: boolean;

    /** Counts of extracted items by category */
    counts: ExtractedCount;

    /** Error message if extraction failed */
    error?: string;

    /** Duration of extraction in milliseconds */
    durationMs: number;
}

/**
 * Knowledge item types that can be extracted from indexer
 */
export type ExtractableKnowledgeType =
    | 'module_info'
    | 'dependency'
    | 'project_context';

/**
 * Declaration kinds that are extracted as module_info
 */
export type ExtractableDeclarationKind =
    | 'module'
    | 'interface'
    | 'package';
```

## Type Usage Examples

### Using ExtractedCount

```typescript
import type { ExtractedCount } from './types.js';

async function extractAndReport(project: ResolvedProject): Promise<void> {
    const counts: ExtractedCount = await extractFromIndex(project, store, options);

    console.log(`Extracted:`);
    console.log(`  - ${counts.modules} modules`);
    console.log(`  - ${counts.interfaces} interfaces`);
    console.log(`  - ${counts.packages} packages`);
    console.log(`  - ${counts.dependencies} dependencies`);
    console.log(`  - ${counts.hierarchy} hierarchy nodes`);
}
```

### Using ExtractionOptions

```typescript
import type { ExtractionOptions } from './types.js';

const options: ExtractionOptions = {
    projectId: 'abc123def456',
    sessionId: crypto.randomUUID(),
    extractModules: true,
    extractDependencies: true,
    extractHierarchy: false,  // Skip hierarchy for large projects
    maxItemsPerCategory: 200,
    excludePatterns: ['**/testbench/**']
};

await extractFromIndex(project, store, options);
```

### Using ExtractionResult

```typescript
import type { ExtractionResult } from './types.js';

function logExtractionResult(result: ExtractionResult): void {
    if (result.success) {
        console.log(`Extraction completed in ${result.durationMs}ms`);
        console.log(`Total items: ${
            result.counts.modules +
            result.counts.interfaces +
            result.counts.packages +
            result.counts.dependencies +
            result.counts.hierarchy
        }`);
    } else {
        console.error(`Extraction failed: ${result.error}`);
    }
}
```

## Type Validation

Consider adding runtime validation for ExtractionOptions:

```typescript
export function validateExtractionOptions(
    options: Partial<ExtractionOptions>
): ExtractionOptions {
    if (!options.projectId) {
        throw new Error('ExtractionOptions.projectId is required');
    }
    if (!options.sessionId) {
        throw new Error('ExtractionOptions.sessionId is required');
    }

    return {
        projectId: options.projectId,
        sessionId: options.sessionId,
        extractModules: options.extractModules ?? true,
        extractDependencies: options.extractDependencies ?? true,
        extractHierarchy: options.extractHierarchy ?? true,
        minConfidence: options.minConfidence ?? 0.9,
        maxItemsPerCategory: options.maxItemsPerCategory ?? 500,
        includePatterns: options.includePatterns,
        excludePatterns: options.excludePatterns
    };
}
```

## Related Types (Reference)

### From KnowledgeStore.ts

```typescript
// Types used by extractors from KnowledgeStore
type KnowledgeType =
    | 'lint_fix'
    | 'code_pattern'
    | 'style_preference'
    | 'module_info'      // ← Used by extractors
    | 'dependency'       // ← Used by extractors
    | 'project_context'  // ← Used by extractors
    | 'test_pattern'
    | 'workflow'
    | 'debug_solution'
    | 'tool_usage';

interface KnowledgeScope {
    global: boolean;
    projectIds?: string[];
    filePatterns?: string[];
    modules?: string[];
}

interface KnowledgeSource {
    method: 'extracted' | 'user_provided' | 'learned' | 'tool_result';
    sessionId?: string;
    filePath?: string;
    tool?: string;
}
```

### From Indexer types

```typescript
// Types from src/indexer/types/index.ts used by extractors
interface ResolvedProject {
    files: FileRecord[];
    declarations: Declaration[];
    references: Reference[];
    instances: Instance[];
    directives: Directive[];
    hierarchy: HierarchyNode[];
    dependencies: FileDependency[];
    // ...
}

interface Declaration {
    id: string;
    name: string;
    kind: DeclarationKind;
    location: { file: string; line: number; col: number };
    parentId?: string;
    // ...
}

type DeclarationKind =
    | 'module'
    | 'interface'
    | 'package'
    | 'port'
    | 'parameter'
    // ... many more
```

## Verification Checklist

- [ ] File created at `src/memory/extractors/types.ts`
- [ ] `ExtractedCount` interface defined with all counters
- [ ] `ExtractionOptions` interface defined with required and optional fields
- [ ] `ExtractionResult` interface defined for return values
- [ ] JSDoc comments added for all types
- [ ] Types exported for use by other modules
- [ ] Optional: Add `validateExtractionOptions()` helper

## Next Steps

After creating this file, proceed to:
- **Module 2.2**: Create module extractor using these types
- **Module 2.3**: Create dependency extractor
- **Module 2.4**: Create hierarchy extractor
